# Agent Instructions

Before editing ROS code, assume the target distro is:

```bash
ROS_DISTRO=lyrical
source /opt/ros/lyrical/setup.bash
```

Keep Hiroz, generated message packages, examples, Docker images, launch files, and validation commands aligned with ROS 2 Lyrical Luth unless the user explicitly asks for a compatibility shim.
